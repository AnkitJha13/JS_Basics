let arr = [1,2,3,4,5]

// arr.push(6)  // [ 1, 2, 3, 4, 5, 6 ] 

// arr.pop()       // [ 1, 2, 3, 4 ]

// arr.splice(2,1);   // [ 1, 2, 4, 5 ]       // basically kitni values hatengi 2nd index se idhar 1 hata hai
// arr.splice(2,3)    // [ 1, 2 ]

// arr.shift()  // deletes 1st value

// arr.unshift(0)  // [ 0, 1, 2, 3, 4, 5 ]    // basically adds the value in the start

console.log(arr);
